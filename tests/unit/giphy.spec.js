import chai from 'chai';
import spies from 'chai-spies';
// import { searchGiphy } from '@/utils/giphy';

chai.use(spies);

describe('giphy.js', () => {
  it('calls the giphy api with the proper query parameters', () => {
    // TODO: test for behavior
  })
});
